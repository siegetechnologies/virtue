These are some helper scripts for creating roles, starting virtues, listing, purging, etc
Sort of an interim admin UI until we have a better looking web server

They're called 'usefules' because awg can't spell

If you want to create a new usefule, look at demoforuser.py or list.py, most of the others
use a lot of outdated boilerplate
